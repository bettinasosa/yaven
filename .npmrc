# Vercel installs with --frozen-lockfile. A dependency added with a different
# package manager updates a different lockfile, so the build fails with
# ERR_PNPM_OUTDATED_LOCKFILE and the deploy silently ships the previous code.
# This repo uses pnpm only — see packageManager in package.json.
engine-strict=true
